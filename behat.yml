default:
  suites:
    default:
      paths:
        - features/user_repository.feature
      contexts:
        - FeatureContext

    e2e:
      paths:
        - features/e2e.feature
      contexts:
        - FeatureContextE2E
